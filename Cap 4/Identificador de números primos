def is_prime(num):
    count = 0
    for i in range(num):
        if num % (i+1) == 0:
            count += 1
    if count == 2:
        return True
    else: return False

#x=int(input("Vamos testar se um número é primo: "))
#y = is_prime(x)


for i in range(1, 200):
 if is_prime(i + 1):
     print(i + 1, end=" ")
print()

